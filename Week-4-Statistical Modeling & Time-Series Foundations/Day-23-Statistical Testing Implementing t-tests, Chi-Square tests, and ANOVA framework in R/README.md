# Day 02: Variable Assignments & Data Types 📊

Today we cover how R stores data in memory. Unlike many languages that use the equal sign (`=`) for assignment, R traditionally uses the arrow operator (`<-`).

## 🧠 Core Concept: The Arrow Assignment (`<-`)
Think of the arrow as an active pipeline routing a data value into a named slot in your workspace. 

```r
# Syntactic Structure:
variable_name <- value