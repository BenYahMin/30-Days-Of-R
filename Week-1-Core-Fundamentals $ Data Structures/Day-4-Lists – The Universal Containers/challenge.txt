Day 4 Practice Challenge
1. Create an R file and inside it:

- Construct a Nested Profile: Create a named list called dataset_report containing an analyst name string, an integer vector of sample sizes, and a nested configuration sub-list.

-Extract and Calculate: Isolate the sample sizes out of the container as a raw numeric vector and calculate its mean.

-Deep Slicing: Write the correct double-bracket or configuration path chain to pull out an item buried deep inside the nested sub-list